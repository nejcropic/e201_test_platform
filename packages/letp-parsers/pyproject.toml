[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"

[project]
name = "letp-parsers"
version = "1.0.3"
description = "Parsers for decoding received binary data from encoders"
authors = [
    { name = "Nejc Ropič", email = "nejc.ropic@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []
