[project]
name = "letp-parsers"
version = "1.0.3"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ropic", email = "nejc.ropic@rls.si" }
]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "twine>=6.2.0",
]
